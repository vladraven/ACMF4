# Теорема (существование, единственность и инвариантность решения отражённой SDDE системы ACMF)

## 1. Постановка

Пусть $\Delta \equiv \Delta_{\max} > 0$, $T > 0$ произвольно, и

$\Omega = \Omega_R \times \Omega_F$, где $\Omega_R = [-S_{buf}, S_{max}]^3$ и $\Omega_F = [0,1]^5 \times [0, F_{max}] \times [0,\infty)^3 \times [0,\infty)$, так что $\Omega \subset \mathbb{R}^{13}$.

Индексы $1,2,3$ — «отражаемые» координаты $SID^{1,2,3}$ (область $\Omega_R$), индексы $4,\dots,13$ — «свободные» координаты $(Inst, Ch, Prod, M, F, Scar, ED^1, ED^2, ED^3, RecDebt)$ (область $\Omega_F$).

**Гипотеза (H1) — регулярность коэффициентов.**

$b: \Omega \times \mathcal{C}([-\Delta,0];\Omega) \to \mathbb{R}^{13}$, $\sigma: \Omega \to \mathbb{R}^{13\times 3}$ измеримы, и существует $K < \infty$ такое, что для всех $x, x' \in \Omega$, $\eta, \eta' \in \mathcal{C}([-\Delta,0];\Omega)$:

$\|b(x,\eta) - b(x',\eta')\| + \|\sigma(x) - \sigma(x')\| \le K(|x-x'| + \|\eta - \eta'\|_\infty)$

$\|b(x,\eta)\| + \|\sigma(x)\| \le K(1 + |x| + \|\eta\|_\infty)$

(Липшицевость и линейный рост требуются и для $\sigma$, не только для $b$.)

**Гипотеза (H2) — inward-pointing drift для свободных координат.**

Для $k = 4,\dots,13$ пусть $\Omega_F^k$ — соответствующий одномерный интервал с нижней границей $\ell_k \in \{0\}$ и (для $k \in \{4,\dots,8\}$) верхней границей $u_k \in \{1, F_{max}\}$. Требуем аналитически, из явной формы $b$:

при $x^k = \ell_k$: $b^k(x,\eta) \ge 0$ для всех $x_{-k} \in \Omega_{-k}$, $\eta$

при $x^k = u_k$: $b^k(x,\eta) \le 0$ для всех $x_{-k} \in \Omega_{-k}$, $\eta$

Дополнительно $\sigma^{k,\cdot}(x) = 0$ при $x^k \in \{\ell_k, u_k\}$.

**Гипотеза (H3) — скачки.**

$J: \Omega \times \mathbb{R}^3 \to \mathbb{R}^{13}$ измерим, $\|J(x,z)\| \le K(1 \wedge |z|)$, и для всех $x \in \Omega$, admissible $z$: $x + J(x,z) \in \Omega$.

Пуассоновская мера $N$ на $\mathbb{R}_+ \times \mathbb{R}^3$ с $\sigma$-конечной интенсивностью $\nu(dz)$, $\int (1 \wedge |z|^2)\, \nu(dz) < \infty$, компенсатор $\widetilde{N} = N - dt\,\nu(dz)$.

Под (H3) скачок никогда не выводит из $\Omega$ — следовательно $L, U$ не взаимодействуют со скачками.

## 2. Теорема

При (H1)–(H3) и $\phi \in \mathcal{C}([-\Delta,0];\Omega)$ существует единственная (с точностью до неотличимости) сильная тройка $(X,L,U)$, адаптированная к фильтрации $(\mathcal{F}_t)$, порождённой $(W,N)$, такая что для $t \ge 0$:

$X_t = \phi(0) + \int_0^t b(X_s,\phi_s)\,ds + \int_0^t \sigma(X_s)\,dW_s + \int_0^t \int_{\mathbb{R}^3} J(X_{s-},z)\,\widetilde{N}(ds,dz) + L_t - U_t$

где $L = (L^1,L^2,L^3,0,\dots,0)$, $U = (U^1,U^2,U^3,0,\dots,0)$ непрерывны, неубывающие, $L_0 = U_0 = 0$,

$\int_0^t \mathbf{1}_{\{X_s^k > -S_{buf}\}}\,dL_s^k = 0$ и $\int_0^t \mathbf{1}_{\{X_s^k < S_{max}\}}\,dU_s^k = 0$ для $k = 1,2,3$,

и $X_t \in \Omega$ п.н. для всех $t \ge 0$.

## 3. Доказательство

### Шаг 1. Многомерная задача Скорохода на $\Omega$ и её липшицевость

$\Omega$ — прямое произведение отрезков и полуосей, отражение действует только по первым трём координатам. Направления отражения на гранях $\Omega_R$ постоянны, поэтому многомерная задача Скорохода декомпозируется покоординатно: для непрерывного $y: [0,T] \to \mathbb{R}^3$ с $y_0 \in [-S_{buf}, S_{max}]^3$ существует единственная пара $(x,\ell,u)$, $x = y + \ell - u \in \Omega_R$, с условиями дополнительности, и отображение (карта Скорохода)

$\Gamma: \mathcal{C}([0,T];\mathbb{R}^3) \to \mathcal{C}([0,T];\Omega_R)$

липшицево непрерывно относительно $\|\cdot\|_\infty$ с константой $C_\Gamma = C_\Gamma(S_{buf}, S_{max})$ (Skorokhod 1961; Anderson–Orey 1976; Tanaka 1979, Prop. 2.1), применённой покоординатно к трём независимым компонентам.

### Шаг 2. Редукция к неподвижной точке (метод Пикара с картой Скорохода)

Определим на $[0,\Delta]$ пространство

$\mathcal{H}_\lambda = \{X \in \mathcal{C}([-\Delta,\Delta];\Omega) : X|_{[-\Delta,0]} = \phi\}$

с нормой $\|X\|_\lambda = (\sup_{t \in [0,\Delta]} e^{-\lambda t}\, \mathbb{E}|X_t|^2)^{1/2}$.

Для $X \in \mathcal{H}_\lambda$ положим $\eta_t = X_{t+\cdot}|_{[-\Delta,0]}$ и определим оператор

$\Phi(X)_t = \Gamma_{4,\dots,13}\Big(\phi(0) + \int_0^t b(X_s,\eta_s)\,ds + \int_0^t \sigma(X_s)\,dW_s + \int_0^t \int J(X_{s-},z)\,\widetilde{N}(ds,dz)\Big)$

где для координат $4,\dots,13$ действие $\Gamma$ тождественно, а для координат $1,2,3$ применяется липшицева карта $\Gamma$ из Шага 1.

Оценка (Ито-изометрия + Липшиц (H1) + Липшиц $\Gamma$ константы $C_\Gamma$ + неравенство БДГ для скачковой части под (H3)) даёт:

$\|\Phi(X) - \Phi(X')\|_\lambda^2 \le \dfrac{C(K, C_\Gamma)}{\lambda}\, \|X - X'\|_\lambda^2$

При $\lambda > C(K, C_\Gamma)$ отображение $\Phi$ — сжатие; по теореме Банаха существует единственная неподвижная точка $X$ на $[0,\Delta]$.

### Шаг 3. Метод шагов (продолжение на $[0,\infty)$)

Получив $X$ на $[-\Delta,\Delta]$, повторяем Шаг 2 с начальной историей $\phi^{(1)} = X|_{[0,\Delta]}$, строя решение на $[\Delta, 2\Delta]$, и так далее индукцией по $n$: решение строится на $[n\Delta, (n+1)\Delta]$ по истории с предыдущего шага. Константа сжатия $\lambda$ не зависит от $n$, поэтому процедура определена для всех $n \in \mathbb{N}$.

### Шаг 4. Недостижимость границ свободных координат и отсутствие взрыва

Для $k = 4,\dots,13$: рассмотрим $Y_t = X_t^k$. По (H2) $b^k \ge 0$ на $\{x^k = \ell_k\}$ и $\sigma^{k,\cdot} = 0$ там же; теорема сравнения для одномерных SDE (Ikeda–Watanabe, Ch. VI, Thm 1.1, сравнение с решением $\equiv \ell_k$) даёт $X_t^k \ge \ell_k$ п.н. для всех $t$. Аналогично для верхних границ $u_k$, где они конечны.

Для координат с $u_k = \infty$ линейный рост (H1) даёт по неравенству Гронуолла:

$\mathbb{E}\,|X_t^k|^2 \le C(1 + \mathbb{E}\|\phi\|_\infty^2)\, e^{Ct} < \infty$ для всех $t < \infty$

что исключает взрыв за конечное время. Совместно с $L^k, U^k \equiv 0$ вне $\{1,2,3\}$ получаем $X_t \in \Omega$ п.н. для всех $t \ge 0$.

### Шаг 5. Единственность

Пусть $(X,L,U)$ и $(X',L',U')$ — два решения с одной и той же историей $\phi$ и одними и теми же $(W,N)$. На $[0,\Delta]$ разность $X - X'$ оценивается тем же неравенством, что в Шаге 2, что даёт $X = X'$ п.н. на $[0,\Delta]$; тогда $L = L'$, $U = U'$ по единственности карты Скорохода $\Gamma$. Индукцией по методу шагов (Шаг 3) единственность распространяется на $[0,\infty)$.

$\blacksquare$